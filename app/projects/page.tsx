import ProjectsClient from '@/components/pages/Corporate/ProjectsClient';

export const metadata = {
  title: 'PROJECTS | DesignDot',
  description: 'Reference work where our hands were (almost) completely untied. WEBSITE, MOBILE APPLICATION, ECOMMERCE, PORTFOLIO SITE, BRAND APPLICATION, ADVERTISING SERVICES.',
};

export default function ProjectsPage() {
  return <ProjectsClient />;
}
